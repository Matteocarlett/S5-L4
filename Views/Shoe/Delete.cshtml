@model Shoe

<h2>Sei sicuro di voler eliminare la scarpa? @(Model.Nome)?</h2>

<form asp-controller="Shoe" asp-action="DeletePost" method="post">
    <input type="hidden" asp-for="@Model.Id" />
    <button>Elimina</button>
</form>
